chrome.devtools.panels.create("fctools",
    "MyPanelIcon.png",
    "../viz/pane/panel.html",
    function(panel) {
      // code invoked on panel creation
      // chrome.devtools.panels.fctools = panel;
    }
);